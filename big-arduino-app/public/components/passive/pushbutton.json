{
  "schemaVersion": "2.1",
  "id": "pushbutton",
  "name": "Tactile Pushbutton",
  "category": "passive",
  "libraryId": "core-basic",
  "modelId": "pushbutton_4pin",
  "primaryProtocol": "digital",
  "supportedProtocols": ["digital", "power"],
  "aliases": ["push button", "button", "tactile switch"],
  "tags": ["starter", "input", "switch"],
  "image": "pushbutton_OFF.png",
  "width": 54,
  "height": 68,
  "description": "Momentary switch with two internally linked sides that connect when pressed.",
  "previewPins": ["PIN1A", "PIN1B"],
  "internalConnections": {
    "always": [
      ["PIN1A", "PIN2A"],
      ["PIN1B", "PIN2B"]
    ],
    "whenPressed": [
      ["PIN1A", "PIN1B"],
      ["PIN2A", "PIN2B"]
    ]
  },
  "pins": [
    {
      "id": "PIN1A",
      "label": "Pin 1A",
      "description": "A-side top pin. Always internally connected to PIN2A (same side). Connects to B-side when button is pressed.",
      "type": "terminal",
      "x": 47,
      "y": 3,
      "hitRadius": 8,
      "functions": [{ "name": "SWITCH_A", "type": "digital" }],
      "electricalRole": "switch-terminal",
      "aliases": ["A1"],
      "required": true
    },
    {
      "id": "PIN1B",
      "label": "Pin 1B",
      "description": "B-side top pin. Always internally connected to PIN2B (same side). Connects to A-side when button is pressed.",
      "type": "terminal",
      "x": 7,
      "y": 3,
      "hitRadius": 8,
      "functions": [{ "name": "SWITCH_B", "type": "digital" }],
      "electricalRole": "switch-terminal",
      "aliases": ["B1"],
      "required": true
    },
    {
      "id": "PIN2A",
      "label": "Pin 2A",
      "description": "A-side bottom pin. Always internally connected to PIN1A (same side). Connects to B-side when button is pressed.",
      "type": "terminal",
      "x": 48,
      "y": 64,
      "hitRadius": 8,
      "functions": [{ "name": "SWITCH_A", "type": "digital" }],
      "electricalRole": "switch-terminal",
      "aliases": ["A2"],
      "required": true
    },
    {
      "id": "PIN2B",
      "label": "Pin 2B",
      "description": "B-side bottom pin. Always internally connected to PIN1B (same side). Connects to A-side when button is pressed.",
      "type": "terminal",
      "x": 7,
      "y": 63,
      "hitRadius": 8,
      "functions": [{ "name": "SWITCH_B", "type": "digital" }],
      "electricalRole": "switch-terminal",
      "aliases": ["B2"],
      "required": true
    }
  ],
  "variants": {
    "off": { "image": "pushbutton_OFF.png" },
    "on": { "image": "pushbutton_ON.png" }
  },
  "knowledgeRefs": {
    "component": "pushbutton",
    "concepts": ["input-pullup"],
    "recipes": ["button-controls-led"]
  },
  "source": {
    "origin": ["big-arduino", "arduino-book", "101-book"],
    "confidence": "high"
  }
}
